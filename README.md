### Usage

1. Create a .env file in the root of the project with the following content:
```sh
export PASSWORD='$6$aJcAVcNj.....'                 # Use: mkpasswd -m sha-512
export PUBLIC_KEY='ssh-ed25519 AAAAC...'   
export PRIVATE_KEY_FILE='D://.ssh//keys//key.pem'
```
2. Run build:
```sh
```